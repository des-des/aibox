{
  "name": "aibox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "istanbul cover tape test && istanbul check-coverage --statement 99",
    "jshint": "jshint -c .jshintrc --exclude-path .gitignore .",
    "watch-build": "babel src/public -d public/js -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "istanbul": "^0.4.2",
    "nodemon": "^1.7.1",
    "pre-commit": "^1.1.1",
    "shot": "^1.7.0",
    "tape": "^4.2.1"
  },
  "pre-commit": [
    "jshint",
    "test"
  ],
  "dependencies": {
    "bcrypt": "^0.8.5",
    "redis": "^2.2.4"
  }
}
